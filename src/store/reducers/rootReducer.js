const innitState = {
    users:  [
        {id: 1, name: "tuan" },
        {id: 2, name: "Bo" }
    ]
}

/* implement to get data from api. */
const rootReducer = (state = innitState, action) => {

    return state;
}

export default rootReducer;